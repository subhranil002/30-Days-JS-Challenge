// Write a program that uses the ternary operator to check if a number is even or odd and log the result to the console

const num = 5;

console.log(num % 2 === 0 ? "Even" : "Odd"); // log "Even" if num is even, log "Odd" if num is odd
